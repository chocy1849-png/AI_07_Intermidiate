# 4/7 작업 계획 (결석 보강)

## 상황 정리

월요일 결석으로 오늘 4/7 분량을 수행한다.
오늘 목표: **청킹 확정 + 임베딩 비교 시작 + Vector DB 초기 구축**

### 현재 상태 확인

| 항목 | 상태 |
|---|---|
| `src/db/vector_store.py` | InMemoryVectorStore 플레이스홀더만 존재. ChromaDB 미구현 |
| `pages/2_Documents.py` | **이미 구현 완료** — 문서 목록, 필터, 파싱 미리보기 포함 |
| ChromaDB | **미설치** |

---

## Track B 오늘 할 일

### [B-3.1] ChromaDB 설치 및 초기화

```bash
pip install chromadb
```

- `requirements.txt`에 `chromadb` 추가
- `data/chroma_db/` 디렉토리를 저장 경로로 사용
- Collection 이름: `rfp_documents`
- Collection 생성 스크립트를 `scripts/init_chroma.py`로 작성

### [B-3.2] `src/db/vector_store.py` 재구현

현재 InMemoryVectorStore를 ChromaDB 기반으로 교체.
구현할 메서드:

```python
def add_documents(chunks: list[dict]) -> None:
    # chunk_id, text, metadata를 Chroma에 upsert
    # metadata 키: file_name, chunk_index, chunk_size, overlap

def query(
    query_embedding: list[float],
    top_k: int = 5,
    filters: dict | None = None,
) -> list[dict]:
    # Chroma where 절로 메타데이터 필터 적용 후 top_k 반환
```

> Track A의 임베딩 모델이 확정되기 전이므로,
> 오늘은 임베딩 없이 더미 벡터([0.0] * 1536)로 upsert 흐름만 먼저 검증한다.

### [B-3.3] 메타데이터 필터링 구현

Chroma의 `where` 절 활용:

```python
# 발주기관 필터 예시
where = {"agency": {"$eq": "한국수자원공사"}}

# 파일형식 필터 예시
where = {"file_format": {"$eq": "hwp"}}
```

- `query()` 메서드에 `filters` 파라미터 연결
- `1_Chat.py`의 사이드바 필터값을 `query()`에 넘기는 흐름 확인

### [B-3.4] `pages/2_Documents.py`

**이미 완료.** 문서 목록, 발주기관 필터, 파싱 텍스트 미리보기까지 구현되어 있음.
추가 작업 없음.

---

## Track A 오늘 할 일 (참고)

| 항목 | 내용 |
|---|---|
| [A-3.1] | 청킹 실험 결과 정성 평가 → 최적 청킹 전략 확정 |
| [A-3.2] | 임베딩 모델 후보 선정: text-embedding-3-small / text-embedding-3-large / BAAI/bge-m3 |
| [A-3.3] | 질문 세트 10개로 Top-5 검색 결과 비교 (수동 평가) |
| [A-3.4] | 임베딩 모델 확정 및 선정 근거 문서화 |

> Track A가 임베딩 모델을 확정하면 Track B의 `query()` 입력 차원(1536 or 3072 or 1024)을 맞춰야 한다.
> 오늘 B-3.2는 더미 벡터로 먼저 구조만 잡고, 모델 확정 후 실제 임베딩으로 교체한다.

---

## 우선순위 순서

1. ChromaDB 설치 및 `requirements.txt` 업데이트
2. `scripts/init_chroma.py` — Collection 생성 확인
3. `src/db/vector_store.py` — `add_documents()` + `query()` 구현
4. 메타데이터 필터(`where` 절) 연결 및 동작 확인
5. Track A 임베딩 모델 확정 결과 수신 → 벡터 차원 맞추기

---

## EOD 목표

- ChromaDB Collection에 더미 벡터로 청크 upsert 성공
- `query()` + 메타데이터 필터 동작 확인
- Track A 임베딩 모델 확정 결과 공유받아 다음 작업 준비
